{
  "name": "systemize-user-api",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\"",
    "clean": "rm -rf dist/",
    "build": "yarn clean && tsc",
    "build:docker": "docker build . -t systemize-user-api",
    "publish:docker": "docker push systemize-user-api",
    "server": "node dist/index.js",
    "watch:build": "yarn clean && tsc --watch",
    "watch:server": "nodemon --inspect=127.0.0.1:9221 './dist/index.js' --watch './dist'",
    "release:major": "changelog -M && git add CHANGELOG.md && git commit -m 'updated CHANGELOG.md' && npm version major && git push origin && git push origin --tags",
    "release:minor": "changelog -m && git add CHANGELOG.md && git commit -m 'updated CHANGELOG.md' && npm version minor && git push origin && git push origin --tags",
    "release:patch": "changelog -p && git add CHANGELOG.md && git commit -m 'updated CHANGELOG.md' && npm version patch && git push origin && git push origin --tags"
  },
  "license": "ISC",
  "dependencies": {
    "@types/mongoose": "^5.0.18",
    "@types/redis": "^2.8.6",
    "axios": "^0.18.0",
    "bluebird": "^3.5.3",
    "body-parser": "^1.18.2",
    "compression": "^1.7.3",
    "cors": "^2.8.4",
    "dotenv": "^6.0.0",
    "express": "^4.16.3",
    "express-validator": "^5.2.0",
    "figlet": "^1.2.0",
    "http-status-codes": "^1.3.0",
    "kafka-node": "^3.0.1",
    "mongodb": "^3.0.4",
    "mongoose": "^5.1.7",
    "morgan": "^1.9.0",
    "nodemon": "^1.18.9",
    "redis": "^2.8.0",
    "ts-node": "^7.0.0",
    "typescript": "^2.9.2"
  },
  "devDependencies": {
    "@types/body-parser": "^1.17.0",
    "@types/express": "^4.16.0",
    "@types/node": "^10.3.6",
    "source-map-support": "^0.5.9"
  }
}
